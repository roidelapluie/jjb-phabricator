- project:
    name: Phabricator
    subproject:
        - libphutil
        - arcanist
        - phabricator
    jobs:
        - {name}-{subproject}-checkout
        - {name}-{subproject}-test
        - {name}-{subproject}-package
